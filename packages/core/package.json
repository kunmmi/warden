{
  "name": "warden-core-marker",
  "private": true,
  "type": "module",
  "description": "Marker only — core ships inside the root merrymen package and is resolved via tsconfig paths (@warden/core)."
}
